spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/farm_management_db_v1_dev
    username: postgres
    password: 123456

  flyway:
    locations: classpath:db/migration_dev

  data:
    redis:
      host: localhost
      port: 6379