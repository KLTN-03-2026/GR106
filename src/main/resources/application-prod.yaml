spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  flyway:
    enabled: false
    locations: classpath:db/migration



  data:
    redis:
      host: localhost
      port: 6379